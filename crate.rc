#[link(name = "mustache",
       vers = "0.3pre",
       uuid = "afecaa07-75c5-466c-b3a3-fae5d32e04aa")];
#[crate_type = "lib"];

extern mod std;

use io::{ReaderUtil, WriterUtil};
use dvec::DVec;
use to_str::ToStr;
use std::map::HashMap;

mod mustache;
